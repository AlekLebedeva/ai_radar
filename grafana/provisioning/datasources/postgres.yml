apiVersion: 1

datasources:
  - name: AI Radar Postgres
    uid: ai_radar_postgres
    type: postgres
    access: proxy
    url: db:5432
    user: postgres
    secureJsonData:
      password: postgres
    jsonData:
      database: ai_radar
      sslmode: disable
      postgresVersion: 1500
      timescaledb: false
    isDefault: true
    editable: true
